from setuptools import setup, find_packages

setup(
    name="medbot",  # no spaces or special characters
    version="0.1.0",  # valid version format
    packages=find_packages(),
    install_requires=[
        # your dependencies
    ],
)
